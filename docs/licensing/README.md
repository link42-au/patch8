# Patch8 source-policy evidence

Status: copied F2 evidence; source-complete research, not legal advice and not runtime capability.

- [`patch8.md`](patch8.md) is the human-readable rights and redistribution register.
- [`source-policy.json`](source-policy.json) is the full Link42 version-1 machine registry copied intact so its digest and
  review history remain comparable. Patch8 uses only records whose `product` equals `patch8`.

The version-1 registry cannot authorize FIRST direct display independently from dataset republication, so FIRST remains
disabled. OSV remains disabled for the first release. P1 performs no upstream request and publishes no upstream data.
